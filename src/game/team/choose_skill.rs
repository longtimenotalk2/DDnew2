// use super::super::unit::Unit;
// use crate::wyrand::Dice;
// use std::fmt::Write;

use super::*;

pub enum Skill {
  Ctrl(i32),
  Punch(i32),
  Kick(i32),
  Untie(i32),
  UntieSelf,
  Escape,
  CtnCtrl,
}

impl Unit {
  
}